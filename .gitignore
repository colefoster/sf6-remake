node_modules/
dist/
*.log
.DS_Store
coverage/
web/assets/
# Byte-identical copies of data/geometry/*.json, regenerated by `npm run geometry`.
# The viewer is served straight out of web/, so it needs them there; the repo does not.
web/*.boxes.json
# Bundled from src/game by `npm run build:play` — compiled, not authored.
web/play.js
data/raw/mmdk/
# Extracted from the game's own CharacterAsset data via MMDK. Regenerate with
# `npm run geometry` — it is not redistributed here. See ATTRIBUTION.md.
data/geometry/
# The third-party snapshot the pipeline used to be pinned to, kept beside the
# live dump so the two readings can still be compared. `fetch-mmdk.mjs` writes
# here. See docs/agents/refresh-the-dump.md.
data/raw/mmdk-2024/
.playwright-mcp/
# Agent worktrees — whole checkouts of this repo, one per background agent.
.claude/worktrees/

# Local secrets
.env
.env.*
!.env.example

# Contact sheets from `npm run figure:sheet` — regenerated, never reviewed as source.
.scratch/sheets/
